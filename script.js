
document.getElementById('inscriptionForm').addEventListener('submit', function(e) {
    e.preventDefault();
    alert("Merci pour votre inscription !");
    // Code pour envoyer les données du formulaire ici (ex: AJAX ou redirection)
});
